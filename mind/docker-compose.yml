version: "3.9"
services:
  backend:
    build: ./mind/Backend
    ports:
      - "8000:8000"
    volumes:
      - ./mind/Backend:/app
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000"]
      interval: 30s
      timeout: 5s
      retries: 5
  frontend:
    build: ./mind/Frontend
    ports:
      - "5173:5173"
    volumes:
      - ./mind/Frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_BASE_URL=http://backend:8000
    depends_on:
      - backend
